import React, { FunctionComponent } from "react";
import styled from "@emotion/styled";
import idBadge from "../../static/svg/icon-id-badge.svg";

const IdBadge: FunctionComponent = () => (
  <div>
    <img src={idBadge} alt="svg" />
  </div>
);

export default IdBadge;
