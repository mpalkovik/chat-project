import React, { FunctionComponent } from "react";
import styled from "@emotion/styled";
import cogs from "../../static/svg/icon-cogs.svg";

const Cogs: FunctionComponent = () => (
  <div>
    <img src={cogs} alt="svg" />
  </div>
);

export default Cogs;
