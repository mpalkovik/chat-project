import React, { FunctionComponent } from "react";
import styled from "@emotion/styled";
import power from "../../static/svg/icon-power-off.svg";

const Power: FunctionComponent = () => (
  <div>
    <img src={power} alt="svg" />
  </div>
);

export default Power;
